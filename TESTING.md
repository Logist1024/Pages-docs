# pages-docs · Cloudflare 真机部署与验收测试指南

> 配套 README.md 使用。目标：**从零在 Cloudflare 控制台部署一次，并把所有功能在真实环境里点一遍**。
> 预计耗时 30–45 分钟（含资源创建与构建等待）。每节末尾有 ✅ 通过标准。

---

## 目录

1. [部署前置准备](#1-部署前置准备)
2. [控制台部署六步（实操版）](#2-控制台部署六步实操版)
3. [/setup 环境自检](#3-setup-环境自检)
4. [验收测试清单](#4-验收测试清单)
   - 4.1 登录与会话
   - 4.2 文档编辑与自动保存
   - 4.3 冲突检测（双开标签页）
   - 4.4 发布流与匿名可见性
   - 4.5 版本历史与回滚（含删除 / 清空）
   - 4.6 图片上传（R2）
   - 4.7 搜索（FTS5）
   - 4.8 SEO 与公开端点
   - 4.9 缓存行为
   - 4.10 备份（Cron）
   - 4.11 安全抽查
   - 4.12 深色 / 浅色模式（阅读站 + 后台）
   - 4.13 站点品牌：favicon、LOGO、页眉公告栏
   - 4.14 发布按钮智能显隐
   - 4.15 面板尺寸拖拽（侧栏 / 编辑分屏）
   - 4.16 站点设置保存
   - 4.17 用量看板
5. [排错速查](#5-排错速查)
6. [验收通过标准](#6-验收通过标准)

---

## 1. 部署前置准备

- [ ] 一个 Cloudflare 账号（免费计划即可跑通全部功能）
- [ ] 一个 GitHub 账号，能 fork 仓库
- [ ] 想好用哪些资源（建议第一次全建齐，测试覆盖最完整）：

| 资源 | 建议 | 免费额度参考 |
|---|---|---|
| D1 数据库 | **必建** | 免费 5GB 存储 / 每天 500 万行读 |
| KV 命名空间 | 建议建 | 免费 10 万读/天 |
| R2 存储桶 | 建议建 | 免费 10GB；**首次使用 R2 需在控制台开通（要绑一张支付方式，不产生费用）** |

> 不建 KV / R2 也能上线：页面缓存退化为 Cache API（发布后最长 60 秒旧页）、图片上传不可用，其余功能不受影响。

---

## 2. 控制台部署六步（实操版）

> ### ⚠️ 不要用「Workers 和 Pages」里的「直接上传」！
>
> 如果你看到「上传的文件超过了 1000 个文件上限」的报错，说明走错了入口：
> 「直接上传」是把**纯静态文件**传到 Cloudflare Pages，而本项目是一个 **Worker**
> （带 Hono API、SSR 阅读页和 D1/KV/R2 绑定）。直接上传静态文件即使成功，
> 得到的也是一个**没有后端的空壳**——登录、编辑、搜索全部不可用。
>
> 另外注意：本项目构建产物只有约 **103 个文件 / 4.1MB**（`dist/client`），
> 报 1000 文件超限几乎肯定是把**整个项目目录（含 node_modules，几万个文件）**
> 拖进去了。任何情况下都不要上传 node_modules。
>
> ✅ 正确路径只有两条：
> - **推荐**：下面六步的「**导入 Git 仓库**」（Workers Builds 云端构建，无需从你电脑上传任何文件）；
> - 备选：本地命令行 `pnpm install && pnpm build` 后 `pnpm deploy`（需 wrangler 登录 + 真实资源 ID 环境变量，见 README「本地开发」）。

**① Fork 仓库**：GitHub 上把本项目 fork 到自己账号。

**② 创建资源并复制 ID**（Cloudflare 控制台 → 左侧「存储和数据库」）：

- [ ] D1 → 创建数据库，名字如 `pages-docs` → 进入详情页复制 **数据库 ID**（UUID 格式）
- [ ] KV → 创建命名空间，如 `pages-docs-cache` → 复制 **命名空间 ID**
- [ ] R2 → 创建桶，如 `pages-docs-media`（只需桶名）

**③ 创建 Worker**：控制台 →「Workers 和 Pages」→ 创建 → **导入 Git 仓库** → 选 fork 的仓库 → 授权 GitHub App。
框架预设应自动识别为 Vite；构建命令保持默认（`pnpm run build`）→ 先别点部署，进入下一步。

**④ 配置 Build Variables**（构建设置 → Variables → 添加）：

| 变量名 | 必填 | 值 | 类型 |
|---|---|---|---|
| `D1_DATABASE_ID` | ✅ | ② 的 D1 数据库 ID | Text |
| `KV_NAMESPACE_ID` | 建议 | ② 的 KV 命名空间 ID | Text |
| `R2_BUCKET_NAME` | 建议 | ② 的 R2 桶名 | Text |

- [ ] 保存 → 点 **部署** → 等构建完成（首次 1–3 分钟）
- [ ] 构建日志确认无报错，Worker 拿到 `https://<worker名>.<子域>.workers.dev` 地址

**⑤ 配置管理员**（Worker → 设置 → 变量和机密 → 添加）：

- [ ] 添加 **Secret**（⚠️ 不是普通 Text 变量）：名字 `ADMIN_CREDENTIALS`，值为：

```text
alice:一个足够强的密码
```

（第一条是 admin；要加编辑就再加一行 `bob:另一密码`。JSON 数组格式见 README。）

**⑥ 自检**：浏览器打开 `https://<你的域名>/setup` → 进入下一节。

✅ **通过标准**：构建成功、Worker 域名能打开、`/setup` 全绿。

---

## 3. /setup 环境自检

打开 `https://<你的域名>/setup`，逐项核对（共 5 项）：

- [ ] **D1 数据库** — ok（连接成功且迁移已执行）
- [ ] **管理员凭据（ADMIN_CREDENTIALS）** — ok，且显示的账号数量与你配置的一致
- [ ] **R2 对象存储（MEDIA）** — ok（未绑则显示 warn，可跳过 4.6）
- [ ] **KV 页面缓存（PAGE_CACHE）** — ok（未绑则 warn，4.9 按 Cache API 模式验收）
- [ ] **登录限流（Rate Limiting binding）** — ok

✅ **通过标准**：必填项全 ok；可选项 ok 或明确 warn（与你是否创建资源一致）。

---

## 4. 验收测试清单

> 把 `<域名>` 替换为你的 Worker 地址。测试账号：`alice`（admin）+ 可选 `bob`（editor）。

### 4.1 登录与会话

| # | 操作 | 预期 |
|---|---|---|
| 1 | 打开 `/admin` | 显示登录表单，无报错 |
| 2 | 输入**错误密码**登录 | 表单内出现「用户名或密码错误」，不跳转 |
| 3 | 连续输错约 10 次 | 出现限流提示（429），约 1 分钟后恢复 ⚠️ 别测太狠，把自己锁 60 秒 |
| 4 | 正确密码登录 `alice` | 进入主界面：左侧文档树 + 顶部工具栏，右上显示 alice |
| 5 | 右上「更多 → 会话管理」 | 列出当前会话（含 alice 的登录时间/IP） |
| 6 | 点「登出」 | 回到登录表单；浏览器后退不能回到管理界面（需重新登录） |

- [ ] 4.1 全部符合

### 4.2 文档编辑与自动保存

| # | 操作 | 预期 |
|---|---|---|
| 1 | 「目录」→ 名称填中文如「指南」，访问路径留空 | 目录创建成功，树里出现「指南」；悬停目录行出现「＋」「✎」「🗑」小按钮（SVG 图标）；访问路径已自动生成（也可手动填英文 slug） |
| 2 | 「文档」→ 所选目录 `guide`，标题「你好」（可中英文） | 创建成功，编辑器打开，树里出现在 guide 目录下（草稿态标识）；访问路径由标题自动生成，无需手填 |
| 3 | 输入一段 Markdown（含标题、列表、代码块），**停手等 3 秒** | 状态栏出现「已保存」（2s 防抖自动保存） |
| 4 | 按 `Ctrl/Cmd + S` | 立即保存，状态更新 |
| 5 | 刷新页面 / 从树里重新点开该文档 | 内容完整恢复 |
| 6 | 修改标题（如「你好世界」），等自动保存 | 树中标题同步更新 |
| 7 | 「更多 → 移动 / 改名」，在「完整访问路径」里直接改路径或选目录 | 树中位置更新，旧路径失效（弹窗里有新地址预览） |
| 8 | 新建一个临时文档 → 删除 | 确认弹窗 → 列表消失 |
| 9 | 悬停空目录行点删除图标 | 目录从树中消失（目录内有文档时会拒绝并提示） |
| 10 | 新建文档时填「自定义访问路径」如 `faq/billing/invoice` | 按自定义路径多级创建成功；留空则回退为按标题自动生成 |

- [ ] 4.2 全部符合

### 4.3 冲突检测（双开标签页）

| # | 操作 | 预期 |
|---|---|---|
| 1 | 同一浏览器开两个标签页 A、B，都登录并打开**同一篇文档** | 两边内容一致 |
| 2 | 在 A 修改第一行并等「已保存」 | — |
| 3 | 在 B 修改**同一行**并等「已保存」 | B 弹出**冲突对话框**：显示服务器当前版本与你的改动的 diff |
| 4 | 选择「采用服务器版本」或合并后保存 | 内容收敛一致，不再报冲突 |

- [ ] 4.3 全部符合（这是核心数据安全特性，务必实测）

### 4.4 发布流与匿名可见性

| # | 操作 | 预期 |
|---|---|---|
| 1 | 在 `guide/hello2` 点「发布」 | 状态变已发布；出现版本历史记录 |
| 2 | **另开无痕窗口**（不登录）访问 `/docs/guide/hello2` | 看到刚发布的内容，页面有站点导航/搜索框；侧栏目录显示后台设置的**目录名称**（如「指南」而非 `guide`） |
| 3 | 回编辑器再改一段，**不发布**，等自动保存 | 无痕窗口刷新仍是**旧发布版**（草稿不外泄） |
| 4 | 登录状态访问 `/docs/guide/hello2` | 看到草稿内容 + 顶部「正在预览草稿」横幅 |
| 5 | 同一 URL 加 `?view=published` | 看到线上发布版 |
| 6 | 点「取消发布」 | 无痕窗口刷新 → 该页下线（404/未发布提示） |
| 7 | 再次「发布」 | 无痕窗口恢复可见 |
| 8 | 查看正文「更新于 …」时间 | JS 可用时显示**浏览器本地时区**时间（悬停可见 UTC 偏移）；禁用 JS 时回退为标注 UTC 的时间 |

- [ ] 4.4 全部符合

### 4.5 版本历史与回滚（含删除 / 清空）

| # | 操作 | 预期 |
|---|---|---|
| 1 | 打开已发布文档 →「历史」 | 抽屉列出每次发布的版本（时间、操作人） |
| 2 | 点某条旧版本查看 | 显示该版本内容 |
| 3 | 点「回滚」 | 文档内容恢复为旧版本并自动发布为新快照；匿名端可见回滚结果 |
| 4 | 再看历史 | 回滚也生成了一条新记录（版本链完整） |
| 5 | 悬停某条**旧**版本记录 | 行尾出现「🗑」删除按钮（仅 admin 可见）；点击并确认 | 
| 6 | 确认删除旧版本 | 该条从历史消失，其余记录不受影响 |
| 7 | 悬停**当前发布快照**那一条点删除 | 被拒绝，提示「该版本是当前发布的快照，请先『更新发布』或『取消发布』后再删除」 |
| 8 | 点抽屉头部「清空历史」并确认 | 除当前发布快照外的全部历史被删除，返回提示清理条数 |
| 9 | 删除后点「回滚」到剩余的当前快照 | 功能正常（当前快照仍可回滚，链路未破坏） |

- [ ] 4.5 全部符合

### 4.6 图片上传（R2，未建 R2 可跳过）

| # | 操作 | 预期 |
|---|---|---|
| 1 | 编辑器里拖入/粘贴一张 PNG | 上传成功，插入 `![](/f/media/...)` 地址 |
| 2 | 无痕窗口打开该 `/f/...` 图片地址 | 图片正常显示（匿名可读） |
| 3 | 拖入一个 `.txt` 或 >10MB 文件 | 被拒绝并提示（类型/大小限制） |

- [ ] 4.6 全部符合

### 4.7 搜索（FTS5）

| # | 操作 | 预期 |
|---|---|---|
| 1 | 无痕窗口打开 `/search`，搜文档标题里的 2–4 字关键词 | 命中结果，摘要里关键词 `<mark>` 高亮 |
| 2 | 编辑器顶栏搜索框输入同关键词 | 下拉出现相同命中，点击跳转 |
| 3 | 搜一个只存在于**草稿**里的词 | 无结果（未发布不进索引） |
| 4 | 搜索页 URL 直接带 `?q=` 分享 | 结果页可直达 |

- [ ] 4.7 全部符合

### 4.8 SEO 与公开端点（无痕窗口/curl）

- [ ] `/docs/guide/hello2` 页面源码含 `<title>`、`canonical`、`og:title`（右键查看源代码，内容是服务端渲染的，不是 JS 拼的）
- [ ] `/sitemap.xml` — 200，列出全部已发布路径，不含 `/admin`、`/api`
- [ ] `/robots.txt` — 200，且 disallow 包含 `/admin`
- [ ] `/feed.xml` — 200，含最新发布文档
- [ ] 首页 `/` — 自动跳转到第一篇已发布文档

### 4.9 缓存行为

| # | 操作 | 预期 |
|---|---|---|
| 1 | 无痕窗口访问某已发布页两次（第二次 F5） | 第二次明显快（缓存命中）；响应头 `Cache-Control: public, max-age=60...` |
| 2 | 发布一次修改 → 无痕窗口立即刷新 | **绑定 KV**：基本立即看到新内容（精准失效）；**未绑 KV**：最长 60 秒后可见（可接受） |
| 3 | （可选）`curl -sI https://<域名>/docs/guide/hello2` 看响应头 | 有 `ETag`；带 `-H 'If-None-Match: <etag>'` 重放返回 **304** |

- [ ] 4.9 全部符合

### 4.10 备份（Cron）

| # | 操作 | 预期 |
|---|---|---|
| 1 | 控制台 → 该 Worker → Cron Triggers（设置里） | 看到 `0 3 * * *` 任务 |
| 2 | 点该任务的 **Run**（立即执行一次） | 执行成功 |
| 3 | 控制台 → R2 → 你的桶 | 出现 `backups/<今天日期>/backup.json` 和 `backup.zip` |
| 4 | 下载 `backup.zip` 解开 | 含 `manifest.json`、`backup.json`、`docs/<path>.md` 全部已发布文档 |

- [ ] 4.10 全部符合

### 4.11 安全抽查

- [ ] 无痕窗口直接 `curl https://<域名>/api/docs` → **401**（未登录拿不到数据）
- [ ] 无痕窗口访问 `/admin` → 只看到登录表单，无任何文档数据泄露
- [ ] 查看阅读页响应头：有 `Content-Security-Policy`、`X-Content-Type-Options: nosniff`
- [ ] 用 `bob`（editor，若配置了）登录：能编辑/发布，但**删除文档、删除版本历史、会话管理、用量看板**不可用（仅 admin）

### 4.12 深色 / 浅色模式（阅读站 + 后台）

| # | 操作 | 预期 |
|---|---|---|
| 1 | 无痕窗口打开任意阅读页 | 默认跟随系统配色；系统深色时页面为深色 |
| 2 | 点页眉右上「☀️/🌙」切换按钮 | 页面立即在深浅间切换，按钮图标随之变化 |
| 3 | 切换后按 F5 刷新 | 主题**保持**且首屏即为所选主题（SSR 直出 `data-theme`，不闪烁）；Cookie 里出现 `pd-theme` |
| 4 | 手动把系统配色改为另一档（或用开发者工具模拟）再刷新 | 页面仍保持手动选择（手动优先于跟随系统） |
| 5 | 打开 `/admin`，点顶栏主题按钮 | 后台在深浅间切换；刷新后保持（localStorage `pd-theme`） |
| 6 | 深色模式下检查代码块 / 表格 / 引用块 | 均使用深色配色，无白底刺眼区域 |

- [ ] 4.12 全部符合

### 4.13 站点品牌：favicon、LOGO、页眉公告栏

| # | 操作 | 预期 |
|---|---|---|
| 1 | 「站点设置」→ 品牌形象：上传一张 PNG/SVG 作 favicon 与 LOGO → 保存 | 弹窗即时显示预览；「恢复默认」可还原内置 LOGO |
| 2 | 打开任意阅读页 | 浏览器标签页图标为新 favicon；页眉站名左侧显示新 LOGO 图 |
| 3 | 打开 `/admin` | 登录页与后台顶栏同样显示新 LOGO 与站点名 |
| 4 | 「站点设置」→ 页眉公告栏：填文案（支持 HTML，如 <code>&lt;strong&gt;</code>）+ 可选链接 → 保存 | 阅读页页眉下方出现公告条，HTML 样式生效；文案含链接时可点跳转 |
| 4b | 「站点设置」→ 页脚：填 HTML（如版权声明）→ 保存 | 每个阅读页底部出现页脚，样式与深浅色主题协调 |
| 5 | 点公告条右侧「×」关闭并刷新 | 公告不再出现（关闭被记住）；更换公告文案后重新显示 |
| 6 | 上传 >200KB 的图片 | 被拒绝并提示大小限制 |

- [ ] 4.13 全部符合

### 4.14 发布按钮智能显隐

| # | 操作 | 预期 |
|---|---|---|
| 1 | 打开一篇**已发布且未改动**的文档 | 工具栏**没有**「更新发布」按钮（避免误点产生冗余版本） |
| 2 | 在正文输入任意字符 | 「更新发布」按钮立即出现 |
| 3 | 删掉刚输入的字符（内容恢复原样后等自动保存） | 按钮再次隐藏（按真实差异判断，不是“改过就算”） |
| 4 | 点「更新发布」发布成功 | 按钮自动隐藏；版本历史新增一条 |
| 5 | 草稿状态文档 | 始终显示「发布」按钮不受影响 |

- [ ] 4.14 全部符合

### 4.15 面板尺寸拖拽（侧栏 / 编辑分屏）

| # | 操作 | 预期 |
|---|---|---|
| 1 | 后台主界面：拖拽侧栏右缘的竖向手柄 | 侧栏宽度实时变化（约 200–560px 之间） |
| 2 | 松手后刷新页面 | 宽度保持（localStorage `pd-sidebar-w`） |
| 3 | 双击侧栏手柄 | 宽度恢复默认 |
| 4 | 打开文档进入分屏模式，拖拽编辑区与预览区之间的分隔条 | 左右比例实时变化（20%–80% 之间），预览随动 |
| 5 | 刷新重开该文档 | 分屏比例保持（localStorage `pd-editor-split-pct`）；双击分隔条复位 50% |

- [ ] 4.15 全部符合

### 4.16 站点设置保存

| # | 操作 | 预期 |
|---|---|---|
| 1 | 打开「站点设置」 | 弹窗底部有吸底的「取消 / 保存设置」按钮（滚动到任何位置都可见） |
| 2 | 清空站点名称直接点「保存设置」 | 出现校验错误「站点名称不能为空」，不发请求 |
| 3 | 填回名称并修改任一项（如导航栏链接）→ 点「保存设置」 | 提示保存成功；关闭重开弹窗，修改仍在 |
| 4 | 打开阅读页 | 站点名 / 导航 / 公告全部为刚保存的值 |

- [ ] 4.16 全部符合

### 4.17 用量看板

| # | 操作 | 预期 |
|---|---|---|
| 1 | admin 登录 `/admin` → 侧栏「用量看板」 | 显示四张卡片：D1 数据库 / R2 对象存储 / KV 页面缓存 / Workers 运行时（数值旁标注「参考值」与免费额度上限） |
| 2 | 查看 D1 卡片 | 显示表数量、总存储、草稿/已发布数；进度条为当前占用相对免费额度比例 |
| 3 | 查看 R2 / KV 卡片 | 未绑定资源时优雅显示「未配置」，不报错 |
| 4 | 查看近 30 天柱状图 | 有发布记录的日期出现柱形；悬停可见当日版本数 |
| 5 | 用 editor 角色（如 bob）访问 `#/usage` | 看板不可见/接口返回 403 或 401（仅 admin） |

- [ ] 4.17 全部符合

---

## 5. 排错速查

> 接口报错先看 toast 末尾方括号里的**稳定错误码**（如 `[DOC_SAVE_NOT_APPLIED]`），
> 到 **[ERRORS.md](ERRORS.md)** 按码查「含义 / 常见原因 / 处理办法」；下表覆盖错误码之外的常见症状。

| 症状 | 原因与处理 |
|---|---|
| 「新建文档→创建」「移动/改名→保存」点击无反应 | 前端 bug（提交按钮未关联表单），已在最新代码修复；拉取/同步仓库后重新部署即可 |
| 「站点设置」点保存无反应 | 同类问题，已修复：保存按钮通过 `form` 属性关联表单并吸底展示；更新代码即可 |
| 删除版本历史被拒绝（409） | 当前发布快照受保护。先「更新发布」产生新快照或「取消发布」后再删 |
| 阅读页切换主题后刷新又变回去 | 浏览器禁用了 Cookie（`pd-theme` 无法持久化）；或站点套了会剥离 Cookie 的 CDN/代理 |
| 用量看板 R2/KV 显示「未配置」 | 对应绑定未创建；按 README 六步补建资源后重新部署即可 |
| 构建失败：`database_id` 不是合法 UUID | Build Variables 缺 `D1_DATABASE_ID` 或填错（要 D1 详情页的 UUID，不是数据库名） |
| 构建失败：找不到模块/命令 | 确认 fork 仓库完整、框架预设为 Vite、构建命令 `pnpm run build` |
| `/setup` 提示 ADMIN_CREDENTIALS 未配置 | 加的是 **Secret** 类型、名字精确为 `ADMIN_CREDENTIALS`；保存后刷新 /setup |
| 登录一直 401 | Secret 值格式：`name:password` 每行一条；注意别带多余空格/引号 |
| 登录突然 429 | 登录限流 10 次/分钟/IP，等 60 秒再试 |
| 发布后匿名看到旧页面 | 未绑 KV 的 Cache API 模式最长 60 秒延迟，属预期；或检查 KV 绑定是否生效 |
| 图片上传报错 | 未建/未绑 R2（`/setup` 会显示 warn），或文件超 10MB / 非图片类型 |
| 中文搜索不准 | FTS5 unicode61 分词局限，用 2–4 字关键词 |
| 改了 Secret 没生效 | Secret 保存即生效；刷新页面重试。旧会话仍有效属正常，可在会话管理吊销 |

---

## 6. 验收通过标准

- [ ] `/setup` 五项检查与资源配置一致（必填全 ok）
- [ ] 4.1–4.5、4.7–4.9、4.11–4.17 全部通过（核心流程 + 新特性）
- [ ] 建了 R2 的：4.6、4.10 也通过
- [ ] 测试数据清理（可选）：删除测试文档，或留着当初始内容

全部通过即验收完成 🎉。之后正常使用：内容变更自动保存，发布即时上线，每晚 03:00 UTC 自动备份。
